<ucb:VMBoundUserControl x:Class="LagoVista.PickAndPlace.App.Controls.PickAndPlace.PickAndPlaceJobControl"
             x:TypeArguments="vm:IJobManagementViewModel"                        
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"              
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ucb="clr-namespace:LagoVista.XPlat;assembly=LagoVista.XPlat.WPF"
             xmlns:vm="clr-namespace:LagoVista.PickAndPlace.Interfaces.ViewModels.PickAndPlace;assembly=LagoVista.PickAndPlace"                           
             xmlns:pcb="clr-namespace:LagoVista.PickAndPlace.App.Controls"
             d:DataContext="{d:DesignInstance vm:IJobManagementViewModel }"
             xmlns:local="clr-namespace:LagoVista.PickAndPlace.App.Controls.PickAndPlace"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>

        <Grid >
            <StackPanel Orientation="Horizontal">
                <ListView Width="200" ItemsSource="{Binding Jobs}" SelectedItem="{Binding SelectedJob, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Label Content="{Binding Name}"></Label>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
                <Button Width="120" Height="32" Style="{StaticResource CommandButton}" Command="{Binding RefreshConfigurationPartsCommand}">Refresh Parts</Button>
                <Button Style="{StaticResource CommandButton}" Command="{Binding ReloadJobCommand}">Reload Job</Button>
                <Button Style="{StaticResource CommandButton}" Command="{Binding SaveCommand}">Save Job</Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DataGrid ItemsSource="{Binding ConfigurationParts}" Grid.Row="0" SelectionMode="Single" SelectedItem="{Binding PlaceablePart, Mode=TwoWay}" CanUserAddRows="false" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Count" Width="80" Binding="{Binding Count}"  />
                    <DataGridTextColumn Header="Package" Width="200" Binding="{Binding PackageName}"  />
                    <DataGridTextColumn Header="Value" Width="200" Binding="{Binding Value}"  />
                    <DataGridTextColumn Header="Component" Width="200" Binding="{Binding ComponentName}"  />
                    <DataGridTextColumn Header="Names" Width="*" Binding="{Binding PartsNames}"  />
                    <DataGridTextColumn Header="Strip Feeder" Width="150" Binding="{Binding StripFeeder}"  />
                    <DataGridTextColumn Header="Strip Feeder" Width="150" Binding="{Binding StripFeeder}"  />
                    <DataGridTextColumn Header="Auto Feeder" Width="150" Binding="{Binding Feeder}"  />
                </DataGrid.Columns>
            </DataGrid>

            <Grid Grid.Row="1" Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <Label Content="{Binding PlaceablePart.PackageName}" ></Label>
                    <Label Content="{Binding PlaceablePart.Value}" ></Label>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource CommandButton}" Content="Part In Feeder"></Button>
                    <Button Style="{StaticResource CommandButton}" Content="Place Selected Part"></Button>
                    <Button Style="{StaticResource CommandButton}" Content="Part In Board"></Button>
                    <Button Style="{StaticResource CommandButton}" Content="Inspect"></Button>
                    <Button Style="{StaticResource CommandButton}" Content="Place All Parts"></Button>
                </StackPanel>
            </Grid>
            
            <DataGrid ItemsSource="{Binding PlaceablePart.Parts}" CanUserAddRows="false" AutoGenerateColumns="false" Grid.Row="2" >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Width="80" Binding="{Binding Name}"  />
                        <DataGridTextColumn Header="X" Width="50" Binding="{Binding X}"  />
                        <DataGridTextColumn Header="Y" Width="50" Binding="{Binding Y}"  />
                        <DataGridTextColumn Header="Angle"  Width="50" Binding="{Binding Rotation}"  />
                    </DataGrid.Columns>
             </DataGrid>
        </Grid>

        <pcb:PCBRenderer Grid.Column="1" TopWiresVisible="true" BottomWiresVisible="true" PCBVisible="true" CenterBoard="true" Grid.Row="2" CircuitBoard="{Binding CircuitBoard}" ></pcb:PCBRenderer>
        
    </Grid>
</ucb:VMBoundUserControl>
