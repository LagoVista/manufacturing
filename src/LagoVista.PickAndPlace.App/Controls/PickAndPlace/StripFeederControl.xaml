<ucb:VMBoundUserControl  x:Class="LagoVista.PickAndPlace.App.Controls.PickAndPlace.StripFeederControl"
             x:TypeArguments="vm:IPartsViewModel"                        
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LagoVista.PickAndPlace.App.Controls.PickAndPlace"
             xmlns:fa="clr-namespace:FontAwesome.WPF;assembly=FontAwesome.WPF"
             xmlns:ucb="clr-namespace:LagoVista.XPlat;assembly=LagoVista.XPlat.WPF"
             xmlns:mch="clr-namespace:LagoVista.PickAndPlace.App.Controls.Machine"
             xmlns:vm="clr-namespace:LagoVista.PickAndPlace.Interfaces.ViewModels.PickAndPlace;assembly=LagoVista.PickAndPlace"                                                                 
             d:DataContext="{d:DesignInstance vm:IPartsViewModel }"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="33*" />
            <ColumnDefinition Width="64*" />
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{Binding StripFeeders}" SelectedItem="{Binding CurrentStripFeeder, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="1" Visibility="{Binding CurrentStripFeeder, Converter={StaticResource NoTNullToVisibilityConverter}}">
            <TextBlock FontSize="32" Text="{Binding CurrentStripFeeder.Name}"></TextBlock>

            <mch:StagingPlateSelectorControl StagePlateLocatedObject ="{Binding CurrentStripFeeder }"></mch:StagingPlateSelectorControl>

            <StackPanel Orientation="Horizontal">
                <StackPanel>
                    <TextBlock Text="Feeder Origin"></TextBlock>
                    <StackPanel Orientation="Horizontal">
                        <Button BorderBrush="Transparent"  Background="Transparent" Command="{Binding SetFeederOriginCommand}"  ToolTip="Set Offset for Feeder Origin"  Width="64" Height="64"  >
                            <Button.Content>
                                <Image Source="/imgs/SetTarget.png"/>
                            </Button.Content>
                        </Button>
                        <Button BorderBrush="Transparent"  Background="Transparent" Command="{Binding GoToFeederOriginCommand}"  ToolTip="Go to feeder origin" Width="64" Height="64"  >
                            <Button.Content>
                                <Image Source="/imgs/MoveToTarget.png"/>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Feeder Reference Hole"></TextBlock>
                        <Button BorderBrush="Transparent"  Background="Transparent" Command="{Binding GoToFeederReferenceHoleCommand}"  ToolTip="Go to feeder origin" Width="64" Height="64"  >
                            <Button.Content>
                                <Image Source="/imgs/MoveToTarget.png"/>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <DataGrid ItemsSource="{Binding CurrentStripFeeder.Rows}" SelectedItem="{Binding CurrentStripFeederRow}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Row Index" Binding="{Binding RowIndex}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Visibility="{Binding CurrentStripFeederRow , Converter={StaticResource NoTNullToVisibilityConverter}}">
                <TextBlock Text="{Binding CurrentStripFeederRow.RowIndex}" />
                <TextBlock Text="{Binding CurrentStripFeederRow.Component.Text}" />
                <Label>Component Category</Label>
                <ComboBox ItemsSource="{Binding ComponentCategories}" DisplayMemberPath="Text" SelectedValuePath="Key" SelectedValue="{Binding SelectedCategoryKey, Mode=TwoWay}"></ComboBox>
                <Label>Component</Label>
                <ComboBox ItemsSource="{Binding Components}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedComponentSummaryId}"></ComboBox>

                <TextBlock FontSize="16" Text="Feeder Row Alignment"></TextBlock>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Move via calculated for approximate location:"></TextBlock>
                    <CheckBox ToolTip="If this is checked, the move will go to the place where the tape hole is expected to be, otherwise it will be the actual value." IsChecked="{Binding UseCalculated }"></CheckBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                        <StackPanel>
                            <TextBlock Text="First Point"></TextBlock>
                            <StackPanel Orientation="Horizontal">
                                <Button BorderBrush="Transparent"  Background="Transparent" Command="{Binding SetFirstFeederReferenceHoleCommand}"  ToolTip="Set Reference Hole for Feeder Row" Width="64" Height="64"  >
                                    <Button.Content>
                                        <Image Source="/imgs/SetTarget.png"/>
                                    </Button.Content>
                                </Button>
                                <Button BorderBrush="Transparent"  Background="Transparent" Command="{Binding GoToFirstFeederReferenceHoleCommand}"  ToolTip="Move To Reference Hole for Feeder Row" Width="64" Height="64"  >
                                    <Button.Content>
                                        <Image Source="/imgs/MoveToTarget.png"/>
                                    </Button.Content>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="Second Reference Hole"></TextBlock>
                            <StackPanel Orientation="Horizontal">
                                <Button BorderBrush="Transparent"  Background="Transparent" Command="{Binding SetLastFeederReferenceHoleCommand}"  ToolTip="Set Reference Hole for Feeder Row" Width="64" Height="64"  >
                                    <Button.Content>
                                        <Image Source="/imgs/SetTarget.png"/>
                                    </Button.Content>
                                </Button>

                                <Button BorderBrush="Transparent"  Background="Transparent" Command="{Binding GoToLastFeederReferenceHoleCommand}"  ToolTip="Move To Reference Hole for Feeder Row" Width="64" Height="64"  >
                                    <Button.Content>
                                        <Image Source="/imgs/MoveToTarget.png"/>
                                    </Button.Content>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Part "></TextBlock>
                    <TextBlock Text="{Binding CurrentPartIndex}"></TextBlock>
                    <TextBlock Text=" out of  "></TextBlock>
                    <TextBlock Text="{Binding TotalPartsInFeederRow}"></TextBlock>

                    <TextBlock Text="Part "></TextBlock>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal">
                    <Button Width="60" Style="{StaticResource CommandButton}" ToolTip="First Part" Command="{Binding GoToFirstPartCommand}" >
                        <Button.Content>
                            <fa:FontAwesome Icon="FastBackward"  />
                        </Button.Content>
                    </Button>
                    <Button Width="60" Style="{StaticResource CommandButton}" ToolTip="Previous Part" Command="{Binding GoToPreviousPartCommand}" >
                        <Button.Content>
                            <fa:FontAwesome Icon="Backward"  />
                        </Button.Content>
                    </Button>
                    <Button Width="60" Style="{StaticResource CommandButton}" ToolTip="Current Part" Command="{Binding GoToCurrentPartCommand}" >
                        <Button.Content>
                            <fa:FontAwesome Icon="CircleOutline"  />
                        </Button.Content>
                    </Button>
                    <Button Width="60" Style="{StaticResource CommandButton}" ToolTip="Next part" Command="{Binding GoToNextPartCommand}" >
                        <Button.Content>
                            <fa:FontAwesome Icon="Forward"  />
                        </Button.Content>
                    </Button>
                    <Button Width="60" Style="{StaticResource CommandButton}" ToolTip="Last part" Command="{Binding GoToLastPartCommand}" >
                        <Button.Content>
                            <fa:FontAwesome Icon="FastForward"  />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </StackPanel>
            <Button HorizontalAlignment="Right" Style="{StaticResource CommandButton}" Command="{Binding SaveCurrentFeederCommand}" Content="Save Feeder"></Button>
        </StackPanel>
    </Grid>
</ucb:VMBoundUserControl>
