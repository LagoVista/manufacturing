<UserControl x:Class="LagoVista.PickAndPlace.App.Controls.UI.Picker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LagoVista.PickAndPlace.App.Controls.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="Label" Style="{StaticResource label}"></TextBlock>
        <ComboBox  FontSize="28" Height="40" SelectionChanged="Value_SelectionChanged" Grid.Row="1" x:Name="Value"></ComboBox>
    </Grid>
</UserControl>
