<Window x:Class="LagoVista.PickAndPlace.App.Views.VisionSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LagoVista.PickAndPlace.App.Views"
        xmlns:vm="clr-namespace:LagoVista.PickAndPlace.Interfaces.ViewModels.PickAndPlace;assembly=LagoVista.PickAndPlace"                                        
        d:DataContext="{d:DesignInstance vm:IVisionProfileViewModel }"
        mc:Ignorable="d"
        xmlns:Controls="clr-namespace:LagoVista.PickAndPlace.App.Controls"
        Title="VisionSettingsView" Height="800" Width="480">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <ComboBox ItemsSource="{Binding VisionProfiles}" SelectedValuePath="Key" SelectedValue="{Binding SelectedProfile}" ></ComboBox>
        <Controls:MVViewOptions Grid.Row="1" VerticalAlignment="Bottom"  />
        <Controls:ImageAdjustments  Grid.Row="2"  />
        <Button Style="{StaticResource CommandButton}" Margin="20" Width="120" Height="32" HorizontalAlignment="Right" Grid.Row="3" Command="{Binding SaveMachineConfigurationCommand}">Save</Button>

    </Grid>
</Window>
